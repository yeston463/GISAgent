# GeoScene Maps SDK for JavaScript 4.29 通过 npm 包 @geoscene/core 加载（ESM），
# 无需 CDN 和 VITE_GEOSCENE_SDK_URL / VITE_GEOSCENE_CSS_URL。
# SDK 资产由 vite.config.js 自动拷贝到 public/assets。

# 天地图开发者 Token（浏览器端类型），从 https://console.tianditu.gov.cn/ 申请。
# 未配置时底图瓦片无法加载（控制台会提示），建筑/分析功能不受影响。
VITE_TIANDITU_TOKEN=

# GIS AI Agent 项目总结

> 2026-07-19 更新：当前主链路已加入 SSE 执行轨迹/决策摘要、天际线与日照筛查、受限动态工具注册、classpath 提示词加载、RAG 启动自动加载、`QWEN-APIKEY` 统一配置和 Windows 一键启动。北京模拟建筑文件已删除。本文后续的限制描述以本更新后的实现为准。

## 一、项目定位

本项目是一个面向城市建筑与规划指标分析的 GIS AI Agent。系统并非只让大模型生成一段文字，而是将自然语言理解、GIS 工具调用、真实空间数据获取、指标计算、地图控制、结果校验和后续建议组合成一条可执行的分析链路。

用户可以通过对话提出“分析某地周边建筑”“计算当前红线范围内的容积率”“对当前上传数据进行规划指标分析”等任务。Agent 会根据任务状态选择地理编码、区域分析、当前视图分析、知识检索或前端地图命令，并在获得真实计算结果后生成专业回复。

当前项目最适合展示的核心场景是：

> 用户输入地点或在地图绘制红线，Agent 自动确定分析区域、获取建筑数据、计算建筑与规划指标、在三维地图展示结果，并给出校验结论和下一步建议。

## 二、系统组成

### 1. Spring Boot Agent 编排层

Java 后端负责接收用户请求、调用大模型进行决策、执行工具、维护分析上下文并向前端返回文字回复和地图命令。

主要模块包括：

- `AgentController`：提供 Agent 对话、偏好保存、历史记录和动态执行接口。
- `AgentLoopService`：实现最多 8 轮的 Observe-Think-Act 循环。
- `DynamicToolRegistry`：统一注册并调用 GIS、知识库和动态工具。
- `ClarificationEngine`：在地点、范围等关键信息不足时主动反问。
- `ValidationLayer`：校验计算状态、建筑数量、用地面积、建筑面积和容积率等结果。
- `SuggestionEngine`：根据指标结果生成后续分析和规划建议。
- `PgVectorMemoryStore`：保存用户偏好、分析历史和长期记忆；数据库不可用时采用内存降级。

### 2. FastAPI GIS 分析引擎

Python 服务承担真正的空间数据处理与指标计算，提供以下接口：

- `/analysis/runtime`：返回当前 GIS 运行环境和可用后端。
- `/analysis/buffer`：根据中心点和半径生成分析范围。
- `/analysis/fetch_buildings`：从 OpenStreetMap Overpass 服务获取建筑轮廓并裁剪到分析区。
- `/analysis/urban_metrics`：基于 AOI 和建筑数据计算城市建筑指标。
- `/analysis/analyze_area`：完成“创建范围—获取建筑—裁剪—计算指标”的一体化流程。

GIS 引擎支持分级运行：

1. 优先使用 ArcPy 专业后端（ArcGIS Pro/GeoScene Pro 解释器）；
2. 其次使用 GeoPandas、Shapely 等开源空间库；
3. 部分环境缺少 GIS 库时，缓冲区仍可使用标准库近似算法降级生成。

GeoScene Enterprise 用于三维成果的展示/发布平台；其服务端指标计算仅在显式启用时使用。

### 3. ArcGIS 三维前端

前端由 Vue 和 ArcGIS Maps SDK 构建，并以静态资源形式集成到 Spring Boot 中。主要能力包括：

- 三维场景浏览与建筑展示；
- 地图点击和要素属性查看；
- 手动绘制规划红线/AOI；
- 将当前 AOI 和建筑数据同步到 Java 后端；
- 执行 Agent 返回的定位、缓冲区、建筑渲染和分析结果展示命令；
- 展示 Agent 回复、澄清问题和后续建议；
- 在服务端取数失败时，将屏幕范围建筑作为后备数据同步给 Agent，再触发重新分析。

### 4. 知识库与记忆

项目包含两类辅助能力：

- **RAG 知识库**：支持上传文档、切分文本、生成向量并进行相似内容检索，可用于补充规范、指标解释和业务知识。
- **用户记忆**：保存用户偏好和历史分析结果，在后续任务中向 Agent 注入相关上下文。

PostgreSQL/pgvector 或 Redis 不属于核心演示链路的强制条件。长期记忆模块具有内存降级机制，但 Redis 会话存储本身仍依赖 Redis 服务。

## 三、完整演示链路

### 场景一：按地点自动分析

1. 用户输入需要分析的地点、城市和范围。
2. Agent 判断信息是否完整；缺少范围时主动追问。
3. Agent 调用地理编码工具获得经纬度。
4. Agent 调用 `analyzeArea`。
5. Python 引擎生成 AOI，并从 OSM 获取真实建筑轮廓。
6. Python 引擎裁剪建筑并计算规划指标。
7. Java 校验指标，收集地图展示命令并生成建议。
8. 前端定位分析区域、绘制范围、加载建筑并展示结果。
9. Agent 输出基于真实工具结果的专业分析结论。

### 场景二：分析当前红线

1. 用户在三维地图上绘制红线。
2. 前端将 AOI 上传到 `/api/gis/upload-context`。
3. 页面自动要求 Agent 分析当前红线区域。
4. Agent 调用 `analyzeCurrentView`。
5. 如果只有 AOI、没有建筑数据，后端会先触发服务端建筑获取。
6. 获取成功后计算指标；失败时才使用当前屏幕建筑作为后备方案。
7. 分析结果和地图命令返回前端展示。

### 场景三：数据同步后的自动复算

当前端收到 `getScreenBuildings` 命令时，会收集可见建筑并上传 GIS 上下文。页面监听 `gis-data-ready` 事件，然后自动再次调用 Agent，让服务端基于新数据重新校验和计算，而不是把前端临时统计直接当作最终结论。

这一机制构成了“Agent 请求数据—前端补充数据—Agent 重新分析”的闭环。

## 四、当前已实现的分析能力

### 1. 空间范围处理

- 根据经纬度和半径生成缓冲区；
- 接收用户绘制的 Polygon AOI；
- 识别和转换 GeoJSON 几何；
- 根据数据位置自动选择适合面积计算的投影坐标系；
- 将建筑裁剪到目标分析范围。

### 2. 建筑数据获取

- 通过 Overpass API 查询 OSM 建筑数据；
- 解析 OSM 节点、路径和建筑属性；
- 支持服务端获取失败后的前端屏幕建筑降级；
- 返回可直接交给地图渲染的 GeoJSON 数据。

### 3. 城市建筑指标

当前计算结果覆盖：

- 分析区用地面积；
- 建筑基底面积；
- 总建筑面积；
- 建筑数量；
- 建筑密度；
- 容积率 FAR；
- 建筑层数分布；
- 建筑高度统计；
- 缺失楼层数据的估算与数据质量信息。

项目会区分真实属性和估算属性，避免在数据不完整时无提示地输出看似精确的结果。

### 4. Agent 决策能力

Agent 当前具备：

- 多轮模型决策；
- 工具选择与参数传递；
- 工具结果观察；
- 失败后切换分析策略；
- 信息不足时反问；
- 禁止无依据编造 GIS 指标；
- 结果校验；
- 主动生成后续建议；
- 将地图操作作为结构化命令返回前端。

因此，该系统已经超出普通“聊天机器人调用单个接口”的形式，形成了具有状态、工具和反馈的有限自治 Agent。

## 五、项目亮点

### 1. 大模型不直接计算 GIS 指标

容积率、面积、建筑数量等数据由 Python GIS 引擎计算，大模型只负责理解需求、选择工具和解释结果。这降低了模型幻觉对专业分析结论的影响。

### 2. 服务端优先的真实数据闭环

项目优先通过服务端从 OSM 获取和处理建筑数据，而不是只统计当前地图上碰巧加载的要素。屏幕建筑仅作为失败后的后备方案，数据来源和计算路径更清晰。

### 3. Agent 与地图双向协同

Agent 不只返回文本，还会产生结构化地图命令；地图也会把红线和建筑数据反向同步给 Agent。系统由此形成“语言驱动地图、地图反馈分析”的双向交互。

### 4. 专业 ArcPy 与开源 GIS 兼容

同一分析接口优先使用 ArcPy 专业后端，不可用时回退到开源 GeoPandas/Shapely 实现，使项目既能利用专业平台，又不完全绑定单一桌面软件环境；GeoScene Enterprise 负责三维成果的展示与发布。

### 5. 具备失败恢复意识

系统对无数据、服务错误、数据缺失和数据库离线设置了不同程度的降级路径，并要求 Agent 在工具失败后更换策略或向用户澄清，而不是直接生成虚构结果。

## 六、当前不足

### 1. Agent 自治程度仍有限

Agent 的可用动作由预先注册的工具和系统提示词限定，最多执行 8 轮。它属于受控的工具型 Agent，还不是可以长期规划和自主扩展任务的通用智能体。

### 2. 动态代码执行不是核心稳定链路

项目保留了动态代码生成和执行接口，但该能力与核心城市建筑分析链路相对独立，且动态生成工具的可靠性、安全性和可解释性不足，不应作为当前演示的主要卖点。

### 3. 外部数据源存在不确定性

OSM 建筑覆盖度、楼层和高度属性因地区而异，Overpass 服务也可能超时或限流。因此，容积率和总建筑面积可能包含楼层估算，展示时应同时说明数据来源与质量。

### 4. 规划评价规则仍较基础

当前系统可以计算指标并进行基础异常判断，但尚未形成按城市、地块性质和规划规范自动匹配控制指标的完整规则库。现有建议更适合作为辅助分析，而不是法定规划审查结论。

### 5. 前后端已解耦，构建产物与源码边界清晰

前端源码唯一真相源位于仓库根目录 `frontend/`，后端不再托管前端构建产物。构建脚本仅验证 `frontend/dist/` 产物，不再同步到 `static/`。前端以独立 Vite 服务运行（`:5173`），通过代理与后端通信。离线演示模式内置 6 栋建筑 + 绿地的固定案例，分析链路完全不依赖 OSM/网络。

### 6. 配置与提示词已完成主链路统一

根目录主链路的 `system.txt` 已重写为 GIS Agent 系统约束，模型调用提示词已移动到 `src/main/resources/prompts/` 并通过 UTF-8 classpath 资源读取。聊天与向量模型统一读取 `QWEN-APIKEY`，聊天模型为 `qwen-plus`。`share/backend-lc4j` 是历史分享快照，不属于 `start-dev.bat` 的运行路径。

### 7. 已增加基础自动化验证

Maven 测试现已覆盖 Spring 上下文启动、工具注册、RAG 自动加载，以及动态 JavaScript 的正常执行与危险 API 拦截。Python 高级分析使用内置案例进行结构化结果验证，前端通过 Vite 生产构建验证 Vue/JavaScript 语法和 ArcGIS 资源复制。

## 七、运行条件

核心演示至少需要：

1. Java 17；
2. Spring Boot 后端运行在 `localhost:8080`；
3. Python FastAPI GIS 服务运行在 `localhost:8000`；
4. 设置 `QWEN-APIKEY` 环境变量；
5. Python 环境安装 FastAPI、Uvicorn 和项目需要的 GIS 库；
6. 网络能够访问大模型接口、地理编码服务和 OSM Overpass 服务。

可选服务：

- PostgreSQL/pgvector：用于持久化用户偏好和分析历史；
- Redis：用于聊天消息存储；
- ArcPy/GeoScene Pro：优先启用专业 GIS 后端；
- GeoPandas/Shapely/PyProj：开源 GIS 计算兜底后端；
- GeoScene Enterprise：三维成果展示/发布。

## 八、建议的比赛演示顺序

1. 打开三维地图，说明系统支持自然语言与地图双向交互。
2. 输入一个明确地点和分析半径，让 Agent 自动完成地理编码和区域分析。
3. 展示真实建筑加载、AOI、容积率、建筑密度、建筑数量和高度/层数统计。
4. 强调指标来自 GIS 引擎而不是大模型生成。
5. 在地图手动绘制红线，触发当前区域自动分析。
6. 展示 Agent 的反问、结果校验和后续建议。
7. 最后说明 ArcPy/开源 GIS 双后端和 OSM/前端建筑数据降级机制，GeoScene 负责三维展示/发布。

演示时应尽量选取已经验证过、OSM 建筑属性较完整的区域，并准备固定地点和固定半径，降低网络和数据质量带来的不确定性。

## 九、后续改进优先级

### 第一优先级：提高演示稳定性

- 固定一套本地建筑和 AOI 演示数据，作为在线数据失败时的确定性后备；
- 在页面显示 GIS 后端、数据来源、估算比例和错误恢复过程；
- 统一 Java、Python 和前端的命令及结果字段定义；
- 清理历史提示词和重复模型配置。

### 第二优先级：增强规划专业性

- 建立按城市、用地类型和地块性质组织的规划规则库；
- 把指标计算、规则匹配、评价和优化建议拆成可追踪步骤；
- 增加日照、可达性、公共服务覆盖、绿地率和空间形态等分析；
- 为每个结论附带数据来源、计算方法和可信度。

### 第三优先级：增强工程能力

- 增加 Agent、GIS 接口和命令协议的自动化测试；
- 将前端源码、构建流程和后端静态资源部署关系规范化；
- 对外部服务增加超时、重试、缓存和限流处理；
- 完善任务日志，让每轮思考、动作、观察和恢复过程可视化。

## 十、总结

本项目已经形成一条能够实际运行的 GIS AI Agent 演示链路：自然语言提出任务，Agent 多轮选择工具，GIS 引擎获取和处理真实空间数据，前端执行地图命令，校验层检查指标，最终输出专业结论和建议。

项目当前最有价值的部分，不是单独的大模型对话、地图页面或指标接口，而是三者已经形成双向闭环。下一阶段应优先提高数据与演示稳定性、强化规划规则依据并统一工程协议，而不是继续无边界增加新的 Agent 概念功能。

根据需求生成受限 JavaScript 数据处理代码。

需求：
{{REQUIREMENT}}

注册时上下文：
{{CONTEXT_JSON}}

运行时可读取：
- params：本次工具调用参数对象
- context：注册工具时保存的只读上下文对象

要求：
1. 只返回纯 JavaScript，不输出代码围栏或解释。
2. 将最终结果赋值给 result。
3. 只做确定性的数组、对象、字符串和数值计算。
4. 禁止文件、网络、进程、线程、反射、Java 类型、包导入、动态求值和无限循环。
5. 返回结果必须可序列化为 JSON。

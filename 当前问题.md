# GISAgent 当前问题清单

更新时间：2026-07-19
适用范围：仓库根目录主运行链路（`start-dev.bat`、Spring Boot、Python GIS、Vue + ArcGIS）。

## 结论先行

当前主链路已经可以构建，核心 Agent、GIS 分析、SSE 执行轨迹、RAG 自动加载和动态工具功能已经接通；但项目还不能直接视为“竞赛交付版”。最大风险集中在竞赛材料缺失、外部服务依赖、数据真实性、密钥管理和缺少完整端到端演示验证。

已验证的基础检查：

- `cmd /c mvnw.cmd -q test` 通过；
- `python -m py_compile main.py cityengine_bridge.py geoscene_publisher.py` 通过；
- 前端 `npm run build` 通过；
- 天际线、日照接口可返回结构化结果；
- 默认前端端口为 `5173`；
- `start-geoscene-services.bat` 已按本机服务名创建，但尚未实际启动三项 GeoScene 服务做完整联调。

## P0：提交或演示前必须处理

### 1. 竞赛材料仍不完整

仓库中没有可直接提交的以下材料：

- 作品说明文档（Word/PDF）；
- 技术报告（正式架构图、创新点、技术难点和实验结果）；
- 3—5 分钟演示视频；
- 高清系统架构图；
- 整理后的演示截图和操作脚本。

`项目总结.md` 和 README 只能作为开发说明，不能替代正式评审材料。

### 2. 密钥已存在泄露风险

用户环境变量截图中出现了 Qwen API Key 和对象存储密钥。截图、聊天记录或日志不应包含任何密钥；已展示过的密钥应立即撤销并重新生成。

当前代码读取：

- `QWEN-APIKEY`：Qwen 聊天和向量模型；
- `AMAP_KEY`：地址地理编码（地址转坐标后执行 `flyTo`）。

仓库的 `.gitignore` 目前没有明确忽略 `.env`，创建本地 `.env` 后需要避免误提交。

### 3. 地址定位仍依赖高德配置

用户说出地址后的流程是“地址解析 → WGS84 坐标校验 → `flyTo`”。当前 `geocodeWithCity` 首选高德；如果没有 `AMAP_KEY`，才会降级到 Qwen 坐标推测。模型推测不能作为可靠 GIS 坐标来源。

更稳妥的替代方案是部署 GeoScene Server Locator，或配置 ArcGIS World Geocoding；目前替代 Locator 尚未接入主链路。

### 4. 完整启动链路还没有在本机跑通一次

已具备：

- `start-dev.bat`：Java、Python、Vue、Redis/pgvector 尝试启动；
- `start-geoscene-services.bat`：Data Store、Portal、Server 管理员权限启动。

仍缺少一次完整验收：新终端/资源管理器双击脚本，确认三个 GeoScene 服务、Python、Java、前端全部就绪，并完成一次地址 `flyTo`、建筑分析和高级分析。Docker 不可用、端口被占用或 GeoScene 服务启动失败时，脚本只能提示降级，不能自动修复安装环境。

## P1：功能和可信度问题

### 5. 在线数据和外部服务不稳定

建筑数据依赖 OSM/Overpass，模型依赖 DashScope，地址解析依赖高德或其他在线服务。当前有基础超时和降级，但没有完整的缓存、重试退避、限流和离线数据包。网络波动会导致评委现场无法复现同一结果。

### 6. 建筑属性质量不确定

OSM 建筑经常缺少楼层或高度属性。系统会估算高度/楼层并标注来源，但估算结果不能作为法定规划指标。屏幕建筑同步也是服务端取数失败后的后备路径，数据覆盖范围和精度取决于当前地图图层。

### 7. 高级分析属于筛查模型，不是法定专项分析

当前天际线和日照实现主要使用建筑中心点、属性高度、太阳位置和几何近似：

- 天际线没有真实地形遮挡和严格通视计算；
- 日照没有窗面、地形、周边遮挡和法规时长判定；
- 阴影图层是估算几何，不等同于 ArcPy/GeoScene 专业审查结果。

页面和报告必须持续标注“方案筛查，不替代法定审查”。

### 8. 演示案例仍是合成数据

`src/main/resources/static/demo-case/case.json` 是上海示例地块和 6 栋演示建筑，规则文件也明确标记为非正式法定规则。它适合离线稳定演示，但不能宣称是真实城市更新项目。若评委要求真实案例，需要补充数据来源、授权说明和可复现数据。

### 9. 多方案比选还不是完整的智能方案生成

前端目前能切换现状、诊断和优化展示状态，后端演示案例主要是“现状 vs 优化”。尚未形成保守、平衡、激进三套方案的独立参数生成、统一评分、约束校验和 Agent 推荐闭环。

### 10. 动态工具仍不适合生产开放使用

动态 JavaScript 已有 API 拦截、长度限制和受限上下文，但仍存在：

- 没有可靠的执行超时、资源配额和取消机制；
- 动态工具只保存在当前 Java 进程，重启即丢失；
- 缺少版本、审计、审批和调用权限；
- `/api/agent/execute` 和动态工具管理接口没有完整的用户认证授权。

该能力适合受控演示，不应让公网用户直接使用。

## P1：工程和验证问题

### 11. RAG 目前是进程内知识库

启动时会自动扫描 `src/main/resources/content` 并向量化，但默认使用内存 `EmbeddingStore`。服务重启后需要重新加载，当前知识文档数量有限，也没有文档版本、重复入库和来源引用管理。PostgreSQL/pgvector 离线时会自动降级，长期记忆也会丢失或只保留临时状态。

### 12. 缺少浏览器级端到端测试

当前测试覆盖 Spring 上下文、动态工具、Python 编译和前端生产构建，但没有自动验证以下完整链路：

```text
输入地址 → 地理编码 → flyTo → 获取建筑 → FAR 计算 → 地图图层 → SSE 轨迹 → 最终报告
```

也没有覆盖 GeoScene Portal/Server、真实 OSM 响应、浏览器权限和服务异常时的 UI 行为。

### 13. 历史分享快照会造成审查混淆

`share/gis-agent-source-share-20260616-1321/backend-lc4j` 仍保留旧提示词、旧模型名和旧模拟资源。README 已说明它不是 `start-dev.bat` 的运行入口，但完整仓库搜索仍会命中这些旧内容。提交竞赛材料前应明确隔离、删除或单独归档历史快照。

### 14. 构建产物和源码边界 — 已解决

前端源码已迁移到仓库根目录 `frontend/`，后端 `src/main/resources/static/` 已清空并不再使用。构建脚本仅验证构建产物，不再同步到 `static/`。历史快照 `share/gis-agent-source-share-20260616-1321/` 已从 Git 移除。`.gitignore` 已覆盖 `target/`、`.venv/`、`__pycache__/`、日志、`/frontend/dist/`、`/frontend/test-results/` 等。

## 建议处理顺序

1. 立即撤销截图中出现过的密钥，重新设置 `QWEN-APIKEY`、`AMAP_KEY`，并将 `.env` 加入忽略规则。
2. 完成 GeoScene 三项服务和 `start-dev.bat` 的一次全流程验收，保存日志和截图。
3. 选定 GeoScene Locator 或 ArcGIS Geocoding 作为高德替代/备用，并测试地址到 `flyTo` 的坐标一致性。
4. 准备真实或有明确来源的演示数据，保留当前上海案例作为离线兜底并明确标注“演示数据”。
5. 补齐作品说明、技术报告、架构图、视频和截图，再整理提交包。
6. 增加浏览器 E2E 测试、动态工具权限和超时控制。
7. 最后处理历史快照隔离和前端静态资源治理。

## 交付前验收清单

- [ ] 密钥已轮换，仓库和截图中无真实密钥；
- [ ] `.env` 已被 Git 忽略；
- [ ] GeoScene Data Store、Portal、Server 均可启动并达到 `RUNNING`；
- [ ] 地址解析成功后能正确 `flyTo`；
- [ ] OSM 失败时页面能明确显示数据来源和降级状态；
- [ ] SSE 执行轨迹、最终报告和高级分析在浏览器中可见；
- [ ] 竞赛文档、视频、架构图和截图已归档；
- [ ] 主链路与历史快照已清楚分离；
- [ ] 全流程测试记录已保存。

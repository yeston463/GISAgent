# Dev/test dependencies for the GIS engine (main.py -> gis/ package).
# The runtime requirements.txt pins fastapi/uvicorn; the GIS libraries
# (shapely/geopandas/pandas/arcpy) are optional at runtime. These tests are
# written to run WITHOUT those optional libraries installed.
-r requirements.txt
pytest>=7.0
pydantic>=2.0
httpx>=0.27
